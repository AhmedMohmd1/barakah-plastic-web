/**
 * Single SheetDB endpoint for all lead-capture forms (Contact form + product
 * quote modal) — consolidated so leads land in one sheet with one schema
 * instead of being split across two.
 */
export const LEAD_FORM_ENDPOINT = 'https://sheetdb.io/api/v1/az68rhltg636u';
